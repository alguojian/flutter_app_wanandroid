name: flutter_app_wanandroid
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  flutter_spinkit: ^3.1.0
  dio: ^2.1.13
  flutter_swiper: ^1.1.6
  flutter_webview_plugin: ^0.3.5
  shared_preferences: ^0.5.3+4
  photo_view: ^0.4.2
  path_provider: ^1.2.0
  share: ^0.6.2+1
  flutter_screenutil: ^0.5.3
  common_utils: ^1.1.3
  oktoast: ^2.2.0
  event_bus: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
  - images/status_ic_state_empty.png
  - images/status_ic_state_error.png
  - images/status_ic_state_loading.png

